{
  "name": "myexpenses",
  "displayName": "MyExpenses",
  "version": "5.0.0",
  "private": true,
  "description": "Offline income and expense tracker",
  "scripts": {
    "build:android": "cordova platform add android@13.0.0 && cordova plugin add cordova-plugin-file@8.1.0 && cordova build android --debug"
  },
  "devDependencies": {
    "cordova": "^12.0.0"
  }
}
